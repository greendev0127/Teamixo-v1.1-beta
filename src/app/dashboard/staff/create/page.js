import { StaffCreateView } from 'src/sections/staff/view';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Dashboard: Create a new staff',
};

export default function StaffCreatePage() {
  return <StaffCreateView />;
}
